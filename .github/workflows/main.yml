# name: Nodejs

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]
#   schedule:
#     - cron: "0 * * * *" # Executa a cada hora
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout código
#         uses: actions/checkout@v4

#       - name: Configurar Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Instalar dependências do sistema
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y \
#             wget \
#             gnupg \
#             ca-certificates \
#             apt-transport-https \
#             xvfb \
#             && sudo rm -rf /var/lib/apt/lists/*

#       - name: Instalar Google Chrome
#         run: |
#           wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add - \
#           && echo "deb [arch=amd64] https://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google.list \
#           && sudo apt-get update \
#           && sudo apt-get install -y google-chrome-stable \
#           && sudo rm -rf /var/lib/apt/lists/*

#       - name: Instalar dependências do Node.js
#         run: npm update && npm install

#       - name: Cookies
#         env:
#           COOKIES: ${{ secrets.COOKIES }}
#           LOCALSTORAGE: ${{ secrets.LOCALSTORAGE }}
#         run: |
#           echo "$COOKIES" > cookies.json
#           echo "$LOCALSTORAGE" > localstorage.json

#       - name: Executar node index.js
#         env:
#           URL: ${{ secrets.URL }}
#         run: xvfb-run --auto-servernum -- node --trace-warnings cpayu.js

#       - name: Atualiza screen.png
#         env:
#           SENHA_PDF: ${{ secrets.SENHA_PDF }}
#         run: |
#           sudo apt-get update && sudo apt-get install -y img2pdf qpdf
#           img2pdf screen.png -o temp.pdf && qpdf --encrypt $SENHA_PDF $SENHA_PDF 256 -- temp.pdf screen.pdf && rm temp.pdf
#           git config --global user.name "github-actions"
#           git config --global user.email "actions@github.com"
#           git add screen.pdf || echo "Nada para adicionar"
#           git commit -m "Atualiza screen.pdf" || echo "Nada para commitar"
#           git push --quiet || echo "Falha no push, provavelmente conflito"


name: Nodejs

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 * * * *" # Executa a cada hora
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            wget \
            gnupg \
            ca-certificates \
            apt-transport-https \
            xvfb \
            && sudo rm -rf /var/lib/apt/lists/*

      - name: Instalar Google Chrome
        run: |
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] https://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          sudo rm -rf /var/lib/apt/lists/*

      - name: Instalar dependências do Node.js
        run: npm install --no-audit --no-fund

      - name: Restaurar cookies e localStorage
        env:
          COOKIES: ${{ secrets.COOKIES }}
          LOCALSTORAGE: ${{ secrets.LOCALSTORAGE }}
        run: |
          echo "$COOKIES" > cookies.json
          echo "$LOCALSTORAGE" > localstorage.json
          echo "✅ Cookies e localStorage restaurados."

      - name: Executar script principal
        env:
          URL: ${{ secrets.URL }}
        run: |
          echo "🚀 Executando cpayu.js..."
          xvfb-run --auto-servernum -- node --trace-warnings cpayu.js
          echo "✅ Execução concluída."
          ls -lh || true

      - name: Verificar screenshot
        run: |
          if [ ! -f screen.png ]; then
            echo "❌ ERRO: screen.png não foi gerado!"
            exit 1
          fi
          echo "✅ Screenshot encontrado:"
          ls -lh screen.png

      - name: Gerar e proteger screen.pdf
        env:
          SENHA_PDF: ${{ secrets.SENHA_PDF }}
        run: |
          set -e
          sudo apt-get update && sudo apt-get install -y img2pdf qpdf
          echo "📸 Convertendo screen.png para PDF..."

          img2pdf screen.png -o temp.pdf

          if [ -s temp.pdf ]; then
            if [ -n "$SENHA_PDF" ]; then
              echo "🔐 Protegendo PDF com senha..."
              qpdf --encrypt "$SENHA_PDF" "$SENHA_PDF" 256 -- temp.pdf screen.pdf
            else
              echo "⚠️ SENHA_PDF não definida, gerando PDF sem senha."
              mv temp.pdf screen.pdf
            fi
          else
            echo "❌ ERRO: temp.pdf está vazio!"
            exit 1
          fi

          rm -f temp.pdf
          echo "✅ PDF gerado:"
          ls -lh screen.pdf

      - name: Commit e push do screen.pdf
        continue-on-error: true
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add screen.pdf || echo "Nada para adicionar"
          git commit -m "📄 Atualiza screen.pdf" || echo "Nada para commitar"
          git push --quiet || echo "⚠️ Falha no push (possível conflito, ignore se agendado)"

      - name: 📤 Upload do PDF como artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screen-pdf
          path: screen.pdf
          retention-days: 7





